<Page x:Class="news.pages.glavnai"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:news.pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="glavnai">

    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.7*"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="0.7*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="1" Grid.RowSpan="3">
                <Button Content="Список агентов" Margin="0 0 0 10" x:Name="listagent" Click="listagent_Click"></Button>
                <Button Content="История реализации продукта, агентом" x:Name="history" Click="history_Click" Margin="0 0 0 10" ></Button>
                <Button Content="Удалить" Margin="0 0 0 10"></Button>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
